# In local development, leave VITE_API_URL empty so Vite's proxy handles /api/v1.
# For production/deployment, set this to your backend server URL (e.g. https://your-backend.railway.app/api/v1).
VITE_API_URL=
